#include<stdio.h>
int main(){
  int num,revrsdNmbr = 0,rmnd;
  printf("Enter an integer: ");
  scanf("%d",&num);
  while (num != 0){
    rmnd = num % 10;
    revrsdNmbr = revrsdNmbr * 10 + rmnd;
    num /= 10;
  }
  printf("Reversed Number = %d", revrsdNmbr);
}
